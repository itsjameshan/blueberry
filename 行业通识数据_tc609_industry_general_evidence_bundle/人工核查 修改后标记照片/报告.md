# 人工核查照片 train/validation/test 重分配报告

> 生成日期：2026-06-25
> 操作：将「人工核查 修改后标记照片」中混在单一 `train/` 文件夹里的 labelme 修正数据，按参照数据集的官方划分，重新拆回 `train/` `validation/` `test/` 三个文件夹。

## 0. 本地路径速查（方便日后查找）

重分配后的三个文件夹（labelme 修正数据，.jpg + .json）：

```
/Users/jameshan/Developments/blueberry/行业通识数据_tc609_industry_general_evidence_bundle/人工核查 修改后标记照片/train/
/Users/jameshan/Developments/blueberry/行业通识数据_tc609_industry_general_evidence_bundle/人工核查 修改后标记照片/validation/
/Users/jameshan/Developments/blueberry/行业通识数据_tc609_industry_general_evidence_bundle/人工核查 修改后标记照片/test/
```

本报告：

```
/Users/jameshan/Developments/blueberry/行业通识数据_tc609_industry_general_evidence_bundle/人工核查 修改后标记照片/报告.md
```

参照数据集（划分基准，.jpg + .txt）：

```
/Users/jameshan/Developments/blueberry/images/yolo_training_ready/image/{train,validation,test}/
/Users/jameshan/Developments/blueberry/images/yolo_training_ready/label/{train,validation,test}/
/Users/jameshan/Developments/blueberry/images/yolo_training_ready/data.yaml
```

> 注意：上级目录名「人工核查 修改后标记照片」含一个空格，命令行操作这些路径时务必整体加引号。

---

## 1. 参照基准

划分以下列数据集为准（ground truth）：

```
/Users/jameshan/Developments/blueberry/images/yolo_training_ready/image/
├── train/        2194 张 .jpg
├── validation/    266 张 .jpg
└── test/          271 张 .jpg            合计 2731 张
```

每张图的 split 归属，由它在参照集中所属的子文件夹决定（文件名完全一致，仅扩展名不同：参照用 `.jpg`+`.txt`，本数据集用 `.jpg`+`.json`）。

## 2. 重分配结果（本文件夹当前状态）

```
人工核查 修改后标记照片/
├── train/        jpg=2193   json=2189
├── validation/   jpg=266    json=265
└── test/         jpg=271    json=270
                  ─────────────────────
   合计           jpg=2730   json=2724
```

- 全部 2730 张 jpg 都成功匹配到某个 split，**无法归类的孤儿图 = 0**。
- 计数自洽：2193+266+271 = 2730 jpg，2189+265+270 = 2724 json。

## 3. 少了哪几张（参照集有、本数据集没有）

共 **1 张**，原属 `train`：

| 文件名 | 所属 split |
|--------|-----------|
| `DSCF5306_JPG.rf.59dfa01270a8e9e8db67b41e5629983f_2_5` | train |

> 因此本数据集 train 为 2193，比参照集 train(2194) 少 1 张。validation、test 数量与参照集完全一致。

## 4. 哪几张没有标记（有 jpg、无对应 json）

共 **6 张**，已随各自 split 归位（仅图片归位，无 json 可移）：

| 文件名 | 所在 split |
|--------|-----------|
| `DSCF5232_JPG.rf.5fd45029c1087da0f3dffdff8be6cf7f_1_7` | train |
| `DSCF5290_JPG.rf.b1bc2d6a5a9165a5903f10549c7fceda_2_5` | train |
| `MSState_YL_Cropped_0078_0_2` | train |
| `MSState_YL_Cropped_0085_0_0` | train |
| `DSCF5300_JPG.rf.094d4907542cf4e7b8d408d45d4371d0_0_7` | validation |
| `MSState_YL_Cropped_0046_0_1` | test |

> 「无标记」可能含义：标注时确认该图无目标（背景图），或漏标。需人工逐张确认；上表即查找清单。

## 5. 其它核对

- **多余的 json（有 json、无 jpg）：0**，标注与图片一一对应，无悬空标注。
- **文件名空格：0**，文件名均为 roboflow 风格哈希，无空格（注意：上级目录名「人工核查 修改后标记照片」含空格，脚本须对路径加引号）。

## 6. 待办（如需补齐到参照集）

1. 从参照集补回缺失的 1 张训练图：
   `images/yolo_training_ready/image/train/DSCF5306_JPG.rf.59dfa01270a8e9e8db67b41e5629983f_2_5.jpg`
   （如需 labelme json，需重新标注或从 `.txt` 转换）。
2. 逐张确认第 4 节 6 张无标记图：背景图保留，漏标则补标。

## 7. 操作可逆性说明

本次为**同盘内移动（mv）**，未删除、未复制、未改动参照集。如需还原为单一 train 文件夹，将 `validation/` 与 `test/` 内文件移回 `train/` 即可。
