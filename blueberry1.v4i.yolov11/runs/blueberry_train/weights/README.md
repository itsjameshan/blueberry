# 训练权重说明

本目录下的 `best.pt` / `last.pt` 为在**网上公开数据集**（Roboflow `blueberry1.v4i.yolov11`）上训练得到的权重，属于 **yolo12 训练之前**的结果（基线版本）。

- `best.pt`：验证集表现最佳的权重
- `last.pt`：最后一个 epoch 的权重
- 对应训练运行：`runs/blueberry_train`

> 仅作历史基线参考，非当前 yolo12 模型。
